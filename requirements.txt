# requirements.txt cho ứng dụng Đánh giá Phương án Kinh doanh

# Thư viện giao diện người dùng
streamlit

# Thư viện xử lý dữ liệu
pandas

# Thư viện cho tính toán tài chính (NPV, IRR)
numpy-financial

# Thư viện cơ sở cho numpy-financial
numpy

# Thư viện cho chức năng AI (Gemini API)
google-genai

# Thư viện để đọc và xử lý file Word (.docx)
python-docx

# Thư viện hỗ trợ đọc/ghi file JSON (cho việc chuyển đổi phản hồi của AI)
# Mặc dù json là built-in, nhưng việc liệt kê đôi khi giúp đảm bảo môi trường.
# Tuy nhiên, ta dùng gói built-in nên không cần liệt kê.

# Thư viện hỗ trợ pandas đọc file Excel (cho các chức năng mở rộng sau này)
openpyxl

# Thư viện hỗ trợ hiển thị DataFrame dưới dạng Markdown
tabulate
